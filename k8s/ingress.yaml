apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: m324-app-ingress
spec:
  rules:
    - host: francescod.m324.hackerman.ch
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: m324-app-service
                port:
                  number: 80
